<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 渐变色 -->
    <gradient
        android:angle="135"
        android:centerColor="@color/mint"
        android:endColor="@color/aqua"
        android:startColor="@color/grass"
        android:type="linear"/>
</shape>